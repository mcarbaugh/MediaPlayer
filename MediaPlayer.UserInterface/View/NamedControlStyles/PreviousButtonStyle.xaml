<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MediaPlayer.UserInterface.View.NamedControlStyles">

    <Style TargetType="Button" BasedOn="{StaticResource ResourceKey=ControlButtonStyle}" x:Key="PreviousButtonStyle">
        <Setter Property="Content" Value="{DynamicResource ResourceKey=PreviousButtonDefault}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Content" Value="{DynamicResource ResourceKey=PreviousButtonMouseOver}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Content" Value="{DynamicResource ResourceKey=PreviousButtonDefault}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>